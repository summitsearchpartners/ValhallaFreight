import {useEffect,useState} from 'react';import {AlertTriangle,CheckCircle2,ReceiptText,WalletCards} from 'lucide-react';import {api} from '../../services/api';import {Card,PageHead,Pill} from '../../components/UI';
export default function FinancePage(){const [rows,setRows]=useState<any[]>([]);const load=()=>api<any[]>('/finance/audit').then(setRows);useEffect(()=>{load()},[]);async function act(id:number,a:string){await api(`/finance/audit/${id}/${a}`,{method:'POST'});load()}const variance=rows.reduce((a,x)=>a+x.variance,0);return <><PageHead eyebrow="FINANCE CENTER" title="Freight Audit & Accounting" sub="Reconcile quoted cost to carrier invoices, protect margin and control AP before customer billing."/><div className="vfKpis"><Card><span>Audit queue</span><b>{rows.length}</b><small>Carrier bills</small></Card><Card><span>Exceptions</span><b>{rows.filter(x=>x.status==='exception').length}</b><small>Needs review</small></Card><Card><span>Open variance</span><b>${variance.toFixed(2)}</b><small>Quoted vs invoiced</small></Card><Card><span>Accounting adapters</span><b>6</b><small>Integration Hub</small></Card></div><Card>{rows.length?<table><thead><tr><th>Bill</th><th>Shipment</th><th>Carrier</th><th>Quoted</th><th>Invoice</th><th>Variance</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(x=><tr><td><b>{x.bill_number}</b></td><td>{x.shipment_number}</td><td>{x.carrier||'—'}</td><td>${x.quoted_cost.toFixed(2)}</td><td>${x.invoice_cost.toFixed(2)}</td><td className={Math.abs(x.variance)>25?'badMoney':''}>${x.variance.toFixed(2)}</td><td><Pill tone={x.status==='approved'?'success':x.status==='exception'?'warning':'info'}>{x.status}</Pill></td><td>{x.status!=='approved'&&<div className="rowActions"><button onClick={()=>act(x.id,'approve')}><CheckCircle2/>Approve</button><button onClick={()=>act(x.id,'dispute')}><AlertTriangle/>Dispute</button></div>}</td></tr>)}</tbody></table>:<div className="emptyStateBig"><ReceiptText/><h3>No carrier bills imported yet</h3><p>Carrier invoice ingestion and audit exceptions will land here.</p></div>}</Card></>}
