import {useEffect,useState} from 'react';import {ShieldAlert,CheckCircle2} from 'lucide-react';import {api} from '../../services/api';import {Card,PageHead,Pill} from '../../components/UI';
export default function ClaimsPage(){const [rows,setRows]=useState<any[]>([]);const load=()=>api<any[]>('/claims').then(setRows);useEffect(()=>{load()},[]);async function resolve(id:number){await api(`/claims/${id}/resolve`,{method:'POST'});load()}return <><PageHead eyebrow="RISK / CLAIMS" title="Claims Center" sub="Manage cargo loss, damage, shortage and service claims against the shipment record."/><div className="vfKpis"><Card><span>Open claims</span><b>{rows.filter(x=>x.status!=='resolved').length}</b><small>Needs action</small></Card><Card><span>Claim exposure</span><b>${rows.filter(x=>x.status!=='resolved').reduce((a,x)=>a+x.amount,0).toLocaleString()}</b><small>Open amount</small></Card><Card><span>Resolved</span><b>{rows.filter(x=>x.status==='resolved').length}</b><small>Closed claims</small></Card><Card><span>Insurance adapters</span><b>3</b><small>Integration Hub</small></Card></div><Card>{rows.length?<table><thead><tr><th>Claim</th><th>Shipment</th><th>Type</th><th>Amount</th><th>Filed</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(x=><tr><td><b>{x.claim_number}</b></td><td>{x.shipment_number}</td><td>{x.claim_type}</td><td>${x.amount.toFixed(2)}</td><td>{new Date(x.filed_at).toLocaleDateString()}</td><td><Pill tone={x.status==='resolved'?'success':'warning'}>{x.status}</Pill></td><td>{x.status!=='resolved'&&<button className="tableAction" onClick={()=>resolve(x.id)}><CheckCircle2/>Resolve</button>}</td></tr>)}</tbody></table>:<div className="emptyStateBig"><ShieldAlert/><h3>No claims recorded</h3><p>Claims linked to shipments will appear here with documents, carrier response and resolution status.</p></div>}</Card></>}
