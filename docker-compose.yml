services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: valhalla_freight
      POSTGRES_USER: valhalla_freight
      POSTGRES_PASSWORD: valhalla_freight
    ports:
      - "5432:5432"
    volumes:
      - valhalla_freight_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U valhalla_freight -d valhalla_freight"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://valhalla_freight:valhalla_freight@db:5432/valhalla_freight
      CORS_ORIGINS: http://localhost:5173
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-valhalla-local-development-secret-change-me}
      DEFAULT_ADMIN_EMAIL: ${DEFAULT_ADMIN_EMAIL:-admin@valhallafreight.local}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-Valhalla123!}
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    environment:
      VITE_API_URL: http://localhost:8000/api/v1
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    depends_on:
      - backend

volumes:
  valhalla_freight_postgres:
